<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>{{ title }}</title>
</head>
<body>
  <h1>{{ title }}</h1>
  <ul>
    {% for ticket in tickets %}
      <li>{{ ticket.title }} — {{ ticket.status }}</li>
    {% endfor %}
  </ul>
</body>
</html>
