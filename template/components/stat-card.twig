<div class="p-6 rounded-2xl border {{ borderColor }} {{ bgColor }} shadow-sm flex items-center justify-between">
  <div>
    <h3 class="text-sm font-medium text-gray-600">{{ title }}</h3>
    <p class="text-2xl font-bold text-gray-900 mt-1">{{ value }}</p>
  </div>
  <div class="p-3 rounded-full {{ color }} bg-opacity-10">
    <i class="{{ icon }} w-6 h-6"></i>
  </div>
</div>

